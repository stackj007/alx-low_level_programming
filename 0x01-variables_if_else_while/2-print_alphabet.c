#include <stdio.h>
/**
*main - Entry point of the program
*
*Description: print the alphabet in lowercase, followed by a new line
*
*Return: Always 0 (Seccsess)
*/
void main(void)
{
char letters;
/* print uppercase letters */
for (letters = 'A'; ch <= 'Z'; ch++)
{
putchar(letters);
}
putchar ('\n');
return (0);
}
